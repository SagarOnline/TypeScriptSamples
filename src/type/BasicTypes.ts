class BasicTypes {
    public static main(): number {
        let age : number = 34;
        console.log('Age is ' + age);
        return 0;
    }


    public static deafultValues(): number {
        let defaultNumber : number;
        console.log('Default Value of number type is  ' + defaultNumber);
        return 0;
    }
}

BasicTypes.deafultValues();